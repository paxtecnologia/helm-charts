apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ms-generic.fullname" . }}-test-connection"
  labels:
    {{- include "ms-generic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ms-generic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
